<h1>New Quiz</h1>
<%= form_for ([@lesson, @quiz]), url: lesson_quizs_path do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
    <p>
    <%= f.label :lesson %><br>
    <%= f.text_field :lesson_id, :value => @lesson.id ,:readonly => true %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Back to Quisz List', lesson_quizs_path(@lesson) %>
