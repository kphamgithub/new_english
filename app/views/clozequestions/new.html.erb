<%= form_for :clozequestion, url: clozequestions_path(:quiz_id => @quiz_id,:lesson_id => @lesson_id ) do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </p>
  <p>
    <%= f.label :instruction %><br>
    <%= f.text_area :instruction %>
  </p>
  <p>
    <%= f.label :question %><br>
    <%= f.text_area :question %>
  </p>
  
 <p>
    <%= f.label :media%><br>
    <%= f.text_field :media %>
    </p>    

<p><%= collection_select :quiz, "id", Quiz.all, :id, :name %></p>

  <p>
  <%= check_box_tag "add_to_quiz" %>Add this question to selected quiz:
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
<%= link_to 'Back to Lessons List', multichoicequestions_path %>
